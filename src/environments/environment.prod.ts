import { Environment } from './environment.model';

// TODO: confirmar la URL real de la API de producción antes de conectar el primer feature.
export const environment: Environment = {
  production: true,
  apiUrl: '',
};
